pipeline {
    agent any

    stages {
        stage('stage build') {
            steps {
                sh 'docker build -t jenkinsfile/testimage:rikayan .'
                echo $?
            }
        }
    }
}
